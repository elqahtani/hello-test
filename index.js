const math = {
  add: (x, y) => {
    return x + y
  }
  substract: (x, y) => {
    return x - y
  },
  multiply: (x, y) => {
    return x * y
  },
  divide: (x, y) => {
    return x / y
  }
}

// console.log(math.add(1, 2));

module.exports = math